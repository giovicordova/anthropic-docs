---
phase: 05-tool-layer-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/search.ts
  - src/tools/list-sections.ts
  - tests/tools.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, TRST-01, TRST-02]
gap_closure: true

must_haves:
  truths:
    - "Searching with source='model' returns only model-tagged pages"
    - "Searching with source='research' returns only research-tagged pages"
    - "Search metadata footer shows model crawl timestamp when model results present"
    - "Search metadata footer shows research crawl timestamp when research results present"
    - "Search metadata footer warns when model or research index is stale"
    - "list_doc_sections displays model pages in a 'Model Pages' group"
    - "list_doc_sections displays research pages in a 'Research Papers' group"
  artifacts:
    - path: "src/tools/search.ts"
      provides: "Search tool with model/research source filter and staleness tracking"
      contains: "model.*research"
    - path: "src/tools/list-sections.ts"
      provides: "List sections tool with model/research page groups"
      contains: "Model Pages"
    - path: "tests/tools.test.ts"
      provides: "Tests covering model/research in search and list tools"
  key_links:
    - from: "src/tools/search.ts"
      to: "src/config.ts"
      via: "MODEL_STALE_DAYS, RESEARCH_STALE_DAYS imports"
      pattern: "MODEL_STALE_DAYS|RESEARCH_STALE_DAYS"
    - from: "src/tools/search.ts"
      to: "src/database.ts"
      via: "getMetadata with model/research timestamp keys"
      pattern: "last_model_crawl_timestamp|last_research_crawl_timestamp"
    - from: "src/tools/list-sections.ts"
      to: "src/database.ts"
      via: "listSections returns model/research rows"
      pattern: 'source === "model"|source === "research"'
---

<objective>
Close INT-01, INT-02, INT-03 integration gaps so model and research content sources are fully surfaced in the search and list-sections MCP tools.

Purpose: Phase 4 added model and research content sources but the tool layer still rejects these values in source filters, tracks staleness against wrong timestamps, and drops pages from list output.
Output: Updated search.ts, list-sections.ts, and tests proving all three gaps are closed.
</objective>

<execution_context>
@/Users/giovannicordova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/giovannicordova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- From src/config.ts — staleness constants already exist -->
export const MODEL_STALE_HOURS = 8;
export const MODEL_STALE_DAYS = MODEL_STALE_HOURS / 24;
export const RESEARCH_STALE_HOURS = 8;
export const RESEARCH_STALE_DAYS = RESEARCH_STALE_HOURS / 24;

<!-- From src/database.ts — metadata keys used by model/research sources (set during crawl) -->
// "last_model_crawl_timestamp" — set by modelSource after crawl
// "last_research_crawl_timestamp" — set by researchSource after crawl
// "model_page_count" — set by modelSource
// "research_page_count" — set by researchSource

<!-- From src/tools/search.ts — current buildMetadataFooter signature -->
export function buildMetadataFooter(stmts: Statements, sources: string[]): string;

<!-- From src/tools/list-sections.ts — current registerListSectionsTool -->
export function registerListSectionsTool(server: McpServer, stmts: Statements, crawl: CrawlManager): void;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add model/research to search source filter and metadata footer</name>
  <files>src/tools/search.ts, tests/tools.test.ts</files>
  <behavior>
    - Test: buildMetadataFooter with model source uses last_model_crawl_timestamp (not last_crawl_timestamp)
    - Test: buildMetadataFooter with research source uses last_research_crawl_timestamp
    - Test: buildMetadataFooter warns when model exceeds MODEL_STALE_DAYS
    - Test: buildMetadataFooter warns when research exceeds RESEARCH_STALE_DAYS
    - Test: model and research each get their own line in footer (not grouped with doc sources)
  </behavior>
  <action>
    In src/tools/search.ts:

    1. Update ALL_SOURCES on line 8: add "model" and "research" to the array.

    2. Update zod enum on line 57: change to ["all", "platform", "code", "api-reference", "blog", "model", "research"].

    3. Update buildMetadataFooter (lines 10-42):
       - Change the docSources filter from `s !== "blog"` to exclude "blog", "model", and "research" (those three each have their own timestamp).
       - Add a `hasModel` check (same pattern as hasBlog). If true, read `last_model_crawl_timestamp` from metadata, compute age, push to parts as `model: last crawled {ts}`, and if age > MODEL_STALE_DAYS push "model" to staleNames.
       - Add a `hasResearch` check. If true, read `last_research_crawl_timestamp`, compute age, push to parts as `research: last crawled {ts}`, and if age > RESEARCH_STALE_DAYS push "research" to staleNames.
       - Import MODEL_STALE_DAYS and RESEARCH_STALE_DAYS from config.ts (add to existing import).

    In tests/tools.test.ts:

    4. In the "staleness metadata" describe block, add tests:
       - "model source uses its own timestamp in footer": set last_model_crawl_timestamp, call buildMetadataFooter with ["model"], assert footer contains "model: last crawled {ts}" and does NOT contain "platform" or "code".
       - "research source uses its own timestamp in footer": same pattern with last_research_crawl_timestamp.
       - "warns when model index is stale": set last_model_crawl_timestamp older than MODEL_STALE_DAYS, assert warning includes "model".
       - "warns when research index is stale": same for research.
       - "model and research not grouped with doc sources": call with ["platform", "model", "research"], assert three separate entries in footer (platform line, model line, research line).
  </action>
  <verify>
    <automated>cd /Users/giovannicordova/Documents/02_projects/anthropic-docs-mcp && npx vitest run tests/tools.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Zod enum accepts "model" and "research" without validation error
    - ALL_SOURCES includes all 6 source types
    - buildMetadataFooter reads model/research timestamps independently from doc/blog
    - Stale warnings fire when model or research exceed their thresholds
    - All new and existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add model/research groups to list-sections tool</name>
  <files>src/tools/list-sections.ts, tests/tools.test.ts</files>
  <behavior>
    - Test: list_doc_sections output contains "Model Pages" group with model-tagged pages
    - Test: list_doc_sections output contains "Research Papers" group with research-tagged pages
    - Test: source filter "model" returns only model pages
    - Test: source filter "research" returns only research pages
  </behavior>
  <action>
    In src/tools/list-sections.ts:

    1. Update zod enum on line 19: change to ["all", "platform", "code", "api-reference", "blog", "model", "research"].

    2. After the blogPages rendering block (line 89), add two new groups:

       Model pages group:
       ```
       const modelPages = sections.filter((s) => s.source === "model");
       if (modelPages.length > 0) {
         output += `## Model Pages (${modelPages.length} pages)\n\n`;
         for (const p of modelPages) {
           output += `- [${p.title}](${p.path})\n`;
         }
         output += "\n";
       }
       ```

       Research papers group:
       ```
       const researchPages = sections.filter((s) => s.source === "research");
       if (researchPages.length > 0) {
         output += `## Research Papers (${researchPages.length} papers)\n\n`;
         for (const p of researchPages) {
           output += `- [${p.title}](${p.path})\n`;
         }
         output += "\n";
       }
       ```

    In tests/tools.test.ts:

    3. Update the formatListSections helper to also handle model and research sources (add modelPages and researchPages filtering + rendering, matching the production code).

    4. In the "tool response format: list_doc_sections" describe block, add a test:
       - "groups model and research pages in their own sections": insert pages with source "model" and "research", call listSections + formatListSections, assert output contains "## Model Pages (1 pages)" and "## Research Papers (1 papers)" and the page titles.

    5. Update the existing grouping test title from "groups pages by source (platform, code, api-reference, blog)" to include model/research, and add model + research page inserts + assertions to it. Or add as separate test — whichever keeps test clarity.
  </action>
  <verify>
    <automated>cd /Users/giovannicordova/Documents/02_projects/anthropic-docs-mcp && npx vitest run tests/tools.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Zod enum on list_doc_sections accepts "model" and "research"
    - Model pages appear under "Model Pages" heading in output
    - Research pages appear under "Research Papers" heading in output
    - All new and existing tests pass
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
cd /Users/giovannicordova/Documents/02_projects/anthropic-docs-mcp && npm test
```

Type-check:
```bash
cd /Users/giovannicordova/Documents/02_projects/anthropic-docs-mcp && npx tsc --noEmit
```

Build:
```bash
cd /Users/giovannicordova/Documents/02_projects/anthropic-docs-mcp && npm run build
```
</verification>

<success_criteria>
1. `source: "model"` and `source: "research"` accepted by search tool without zod validation error
2. Search metadata footer shows model-specific and research-specific crawl timestamps
3. Stale warnings trigger independently for model and research based on their own thresholds
4. list_doc_sections renders model pages under "Model Pages" and research under "Research Papers"
5. npm test passes with 0 failures
6. npx tsc --noEmit passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tool-layer-integration/05-01-SUMMARY.md`
</output>
